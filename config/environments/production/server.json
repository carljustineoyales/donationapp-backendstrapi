{
  "host": "${process.env.APP_HOST || 'localhost'}",
  "port": "${process.env.PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": true,
    "ssl": true,
    "host": "radiant-hollows-04485.herokuapp.com",
    "port": 443
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": true
  }
}
