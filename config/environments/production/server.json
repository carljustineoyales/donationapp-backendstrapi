{
  "host": "${process.env.APP_HOST || 'localhost'}",
  "port": "${process.env.PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": false,
    "ssl": true,
    "host": "https://radiant-hollows-04485.herokuapp.com/",
    "port": "${process.env.PORT || 1337}"
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": true
  }
}
